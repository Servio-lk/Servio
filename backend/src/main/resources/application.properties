cors.allowed-origins=${FRONTEND_URL:http://localhost:5173}
spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5433}/${DB_NAME:servio_db}?prepareThreshold=0
spring.datasource.username=${DB_USER:servio_user}
spring.datasource.password=${DB_PASSWORD:servio_password}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

server.port=${PORT:3001}

# JWT Configuration
jwt.secret=${JWT_SECRET:3q4w5e6r7t8y9u0i1o2p3a4s5d6f7g8h9j0k1l2z3x4c5v6b7n8m9QWERTYUIOP}
jwt.expiration=604800000

# CORS Configuration
cors.allowed-origins=${FRONTEND_URL:http://localhost:5173,http://localhost:3000,http://localhost,http://localhost:80}
