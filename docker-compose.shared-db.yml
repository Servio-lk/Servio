services:
  # Backend API (Spring Boot) - Using Shared/Remote Database
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: servio-backend
    restart: always
    environment:
      # Remote/Shared Database Configuration (update these with your shared DB credentials)
      - DB_HOST=${SHARED_DB_HOST:-your-shared-db-host.com}
      - DB_PORT=${SHARED_DB_PORT:-5432}
      - DB_USER=${SHARED_DB_USER:-your_db_user}
      - DB_PASSWORD=${SHARED_DB_PASSWORD:-your_db_password}
      - DB_NAME=${SHARED_DB_NAME:-servio_db}
      # JWT Configuration
      - JWT_SECRET=3q4w5e6r7t8y9u0i1o2p3a4s5d6f7g8h9j0k1l2z3x4c5v6b7n8m9QWERTYUIOP
      - JWT_EXPIRATION=604800000
      # CORS Configuration
      - CORS_ALLOWED_ORIGINS=http://localhost,http://localhost:80,http://localhost:5173
      - FRONTEND_URL=http://localhost:5173,http://localhost:3000,http://localhost,http://localhost:80
      # Supabase Configuration
      - SUPABASE_URL=https://szgvnurzdglflmdabjol.supabase.co
      - SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InN6Z3ZudXJ6ZGdsZmxtZGFiam9sIiwicm9sZSI6ImFub24iLCJpYXQiOjE3Njg4OTc3NTgsImV4cCI6MjA4NDQ3Mzc1OH0.MeQsJIGJ-Fy9GLrcHg3M1rM-eEIpmKKsRsrVYPI6U6E
    ports:
      - "3001:3001"
    networks:
      - servio-network

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: servio-frontend
    restart: always
    ports:
      - "80:80"
    depends_on:
      - backend
    environment:
      - VITE_API_URL=http://localhost:3001/api
      - VITE_SUPABASE_URL=https://szgvnurzdglflmdabjol.supabase.co
      - VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InN6Z3ZudXJ6ZGdsZmxtZGFiam9sIiwicm9sZSI6ImFub24iLCJpYXQiOjE3Njg4OTc3NTgsImV4cCI6MjA4NDQ3Mzc1OH0.MeQsJIGJ-Fy9GLrcHg3M1rM-eEIpmKKsRsrVYPI6U6E
    networks:
      - servio-network

networks:
  servio-network:
    driver: bridge
